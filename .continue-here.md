---
context: default
work: superpowers Subagent-Driven Development (SDD) execution
plan_a: docs/superpowers/plans/2026-06-29-jobtype-registry.md
plan_b: docs/superpowers/plans/2026-06-29-lifecycle-field-hooks.md
ledger: .superpowers/sdd/progress.md   # durable, machine-checkable resume map (gitignored)
task: A6 of A1..A10 (then Plan B B1..B7)
status: in_progress
last_updated: 2026-06-29T18:04:21Z
branch: feat/core-extension-hooks
head_at_pause: d47d11b
---

# Resume Here — SDD execution of two core-hook plans

This is NOT a GSD `.planning/` project. Work is being driven by the
**superpowers:subagent-driven-development** skill: one implementer subagent per
plan task, a task reviewer after each, then a final whole-branch review. The
**authoritative resume map is the ledger** `.superpowers/sdd/progress.md` — trust
it and `git log` over memory.

## ⚠️ Critical: A6 landed during the pause but is NOT yet reviewed

The Task A6 implementer (which was mid-flight at pause) finished and committed
`c554c41` — `feat(core): build job type pickers from JobTypeRegistry`. Tree is
clean. But work was paused **before A6's task-review ran**, so A6 is implemented
but unverified by a reviewer.

Note the commit order: `d47d11b` (A5) → `94a94d8` (this WIP handoff) → `c554c41`
(A6). The WIP handoff commit interleaved, so to review ONLY A6's diff use BASE
`94a94d8` (A6's parent), not `d47d11b`.

**First action on resume:** review A6 before continuing —
`<skill>/scripts/review-package 94a94d8 c554c41`, then dispatch the Task A6 reviewer
(global constraints + brief `.superpowers/sdd/task-6-brief.md`). Mark A6 complete in
the ledger only after it approves; then proceed to A7. A6 implementer notes:
admins bypass `EnsureDsgvoInformed` (no dsgvo fields needed for the admin-route
test); the brief's Step 6 regression filter `JobController|PortalJob|ManualJob`
matched zero files — `--filter=JobType` (9 tests, green) was used as the proxy.

<current_state>
Plan A (JobType Registry, 10 tasks): **A1–A5 complete, reviewed clean, all on
branch** (commits c5fbca5, 6888147, bd5b3c7, cbfc4d5, d47d11b). A6 dispatched,
unverified (see above). A7–A10 not started. Plan B (Lifecycle Field Hooks,
7 tasks) not started — begins after Plan A finishes (B changes the same
`JobLifecycleService` methods A4 touched, adding `array $extra`).
</current_state>

<completed_work>
- A1: JobTypeValue + JobTypeRegistry — done (c5fbca5)
- A2: JobTypeCast + Job::type cast; also added the JobTypeRegistry singleton
  binding in AppServiceProvider (prereq for label()) — done (6888147)
- A3: registerCoreJobTypes() at boot (did NOT re-add the A2 singleton) — done (bd5b3c7)
- A4: service+controllers JobType enum → string $type — done (cbfc4d5)
- A5: registry-backed validation (Rule::in(registry->values())) — done (d47d11b)
- A6: dispatched, unverified
</completed_work>

<remaining_work>
- A6: picker swaps in 4 controllers + dashboard.blade.php — VERIFY or re-dispatch
- A7: monthly_statistics → `counts` JSON migration + MonthlyStatistic model
- A8: RetentionService aggregateForMonth dynamic counts
- A9: example-module demo JobType (gruenpflege)
- A10: default-behavior guard + full suite
- Plan B B1–B7: LifecyclePoint + LifecycleFieldRegistry, persist savepoints,
  @lifecycleFields directive, service $extra threading, 4 dashboard slots,
  example-module demo field, default-behavior guard. (Generate briefs with the
  skill's task-brief script against plan_b; record a Plan B BASE = Plan A's final HEAD.)
- Final: whole-branch code review (opus) → superpowers:finishing-a-development-branch
</remaining_work>

<decisions_made>
- Mechanism = dedicated LifecycleFieldRegistry (Variant B), not event-carried payloads.
- Handler failure policy = resilient + loud logging (per-handler savepoint).
- JobType: value-object cast (zero view churn); enum kept as source of 4 core types;
  monthly_statistics redesigned to JSON counts NOW (not deferred).
- Implementers/reviewers run on `sonnet`; final whole-branch review on `opus`.
</decisions_made>

<blockers>
- None. Only the A6 verification ambiguity above.
</blockers>

## Required Reading (in order)
1. `.superpowers/sdd/progress.md` — the durable ledger (task status + learnings).
2. `docs/superpowers/plans/2026-06-29-jobtype-registry.md` — Plan A (tasks A6–A10 remain).
3. `docs/superpowers/specs/2026-06-29-lifecycle-hooks-und-jobtype-registry-design.md` — the approved design.
4. The superpowers:subagent-driven-development SKILL — the execution loop + helper scripts (task-brief, review-package) in its `scripts/` dir.

## Critical Anti-Patterns (do NOT repeat)
- TEST RUNNER: judge pass/fail with `./vendor/bin/phpunit --filter=<Name>` (pristine).
  `php artisan test` emits a pre-existing unrelated `file_get_contents` warning here — NOT a defect.
- DSGVO GATE: any test hitting authenticated driver/admin web routes must set the user's
  `dsgvo_informed_at = now()` + confirmed version, or `EnsureDsgvoInformed` redirects before
  the code under test runs (pattern: `tests/Feature/.../SlotRegistryTest.php`). Relevant for
  Plan B Tasks B4/B5 (driver-route posts).
- DO NOT re-dispatch a ledger-complete task. A1–A5 are done; their commits exist in git.

## Infrastructure State
- Tests: sqlite `:memory:` (no external DB). Branch `feat/core-extension-hooks`.
- Plans/spec/ledger live under gitignored `docs/superpowers/` and `.superpowers/` — local only.

<context>
Building two additive core hooks ahead of the (not-yet-built) Lager + Grünpflege
modules, same philosophy as the prior GpsPointReceived/PublicHomepageRegistry commit.
Plan A must land before Plan B (shared JobLifecycleService signatures). Each task is
TDD; each plan ends with example-module verification + a default-behavior-unchanged test.
</context>

<next_action>
Review A6 (commit c554c41, BASE 94a94d8) — see the ⚠️ block — then continue the
SDD loop: mark A6 complete on approval, then A7→A10, then Plan B.
</next_action>
